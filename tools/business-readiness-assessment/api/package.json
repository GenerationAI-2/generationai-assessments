{
  "name": "business-readiness-assessment-api",
  "version": "1.0.0",
  "description": "Azure Functions API for Business Readiness Assessment",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "dev": "npm run build && func start",
    "clean": "rm -rf dist",
    "test": "echo \"No tests yet\""
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "@generation-ai/types": "workspace:*",
    "@generation-ai/utils": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
